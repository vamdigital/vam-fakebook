import { fetchError } from "@/app/utils/fetchError";

export default async function InvoicesPage() {
  // await fetchError();
  return (
    <section className="flex flex-col w-full">
      <h2>List of invoices are there for you to click on and check</h2>
    </section>
  );
}
