import { PageTitle } from "@/app/components/PageTitle";

export default function DepositsPage() {
  return (
    <section className="flex">
      <PageTitle pageTitle="Deposits" />
    </section>
  );
}
